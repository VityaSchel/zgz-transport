name: Publish (JSR)
on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish:
    runs-on: node-latest

    permissions:
      contents: read
      id-token: write

    defaults:
      run:
        working-directory: lib/javascript

    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun i --frozen-lockfile

      - name: Copy license
        run: cp ../../LICENSE .

      - name: Publish package
        run: bunx --bun jsr publish --token ${{ secrets.JSR_TOKEN }}
